#!/usr/bin/perl -w

# create-diamonds.pl  is a script  to take  ordinary systems  and find
# points where  they do  not conform to  the Diamond API,  and suggest
# possible changes.

# detect all prints instead of Message
# detect all Term::ReadKey stuff
# detect all <STDIN> stuff
